BOT_TOKEN=              # Telegram bot token from @BotFather
ALLOWED_USER_ID=        # Your Telegram user ID (numeric)
DEFAULT_CWD=~           # Default working directory (before workspaces are configured)
GH_TOKEN=               # Optional: GitHub token for cloning/pushing HTTPS repos
DAILY_HOUR=8            # Hour (0-23) to run the daily routine
DAILY_PROMPT=Run the /start-my-day skill  # Prompt sent to Claude in the Daily topic
